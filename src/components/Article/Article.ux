<template>
    <div class="articlebg">
        <div style="width: 89%; flex-direction: column">
          <text class="articletitle">{{ article.title }}</text>
          <text if="{{showCategory}}" style="font-size: 20px; font-weight: 600; color: #9e9e9e; margin-top: 6px">
              类型：{{ article.category_name }}
          </text>
        </div>
    </div>
</template>

<script>
export default {
  props: {
    article: {
      default: {}
    },
    showCategory: {
      default: ""
    }
  },
  onInit(){
    if(this.showCategory){
      this.showCategory = true
    }
    else{
      this.showCategory = false
    }
  }
}
</script>

<style lang="less">
@import '@less/global.less';

.articlebg {
  width: 72%; 
  min-height: 120px; 
  max-height: 120px; 
  justify-content: center;
  align-items: center;
  margin-top: 10px; 
  background-color: #262626af;
  flex-direction: column;
  border-radius: 24px;
}

.articletitle {
  font-size: 24px;
  font-weight: 600;
  max-height: 70px;
  text-overflow: ellipsis;
}
</style>